server:
  port: 9527

eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:9527/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 0